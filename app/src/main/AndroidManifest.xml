<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bukagambar">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StepSatuActivity" />
        <activity android:name=".NamaBarang" />
        <activity android:name=".KategoriBarangActivity" />
        <activity android:name=".DeskripsiBarang" />
        <activity android:name=".DetailBarang" />
        <activity android:name=".SpesifikasiBarang" />
        <activity android:name=".PengirimanBarang" />
        <activity android:name=".Test_Gallery_Activity"></activity>
    </application>

</manifest>